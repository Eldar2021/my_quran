name: gallery
description: "A new Flutter project."
resolution: workspace
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons:
  pinput:

  mq_app_ui:
  mq_storage:

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail:

flutter:
  uses-material-design: true
