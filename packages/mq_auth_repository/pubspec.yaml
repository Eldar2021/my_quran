name: mq_auth_repository
description: MyQuran Auth Repository
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=3.7.0-0 <4.0.0"
  flutter: ^3.29.0

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.16.0
  json_annotation: ^4.9.0
  firebase_auth: ^5.4.2
  google_sign_in: ^6.2.2
  sign_in_with_apple: ^6.1.4

  mq_crashlytics:
    path: ../mq_crashlytics
  mq_storage:
    path: ../mq_storage
  mq_remote_client:
    path: ../mq_remote_client

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  very_good_analysis: ^7.0.0
  build_runner: ^2.4.15
  json_serializable: ^6.9.4
