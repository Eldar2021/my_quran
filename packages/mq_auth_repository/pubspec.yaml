name: mq_auth_repository
description: Mq Auth Repository
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.5.0
  flutter: ^3.24.0

dependencies:
  flutter:
    sdk: flutter 
  # plugins
  google_sign_in: ^6.2.2
  sign_in_with_apple: ^6.1.4
  # packages
  meta: ^1.15.0
  json_annotation: ^4.9.0
  # local packages
  mq_remote_client:
    path: ../mq_remote_client
  mq_storage:
    path: ../mq_storage

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  very_good_analysis: ^6.0.0
  json_serializable: ^6.9.0
  build_runner: ^2.4.13
