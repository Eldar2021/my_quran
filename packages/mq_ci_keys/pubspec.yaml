name: mq_ci_keys
description: A starting point for Dart libraries or applications.
version: 1.0.0

environment:
  sdk: ">=3.2.0 <4.0.0"

dev_dependencies:
  very_good_analysis: ^5.1.0
  test: ^1.25.0
