name: mq_analytics
description: MyQuran Analytics package
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=3.7.0-0 <4.0.0"
  flutter: ^3.29.0

dependencies:
  flutter:
    sdk: flutter
  firebase_analytics: ^11.3.3
  mq_crashlytics:
    path: ../mq_crashlytics

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  very_good_analysis: ^6.0.0
