name: mq_crashlytics
description: MyQuran Crashlytics package
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=3.7.0-0 <4.0.0"
  flutter: ^3.29.0

dependencies:
  flutter:
    sdk: flutter
  firebase_crashlytics: ^4.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  very_good_analysis: ^7.0.0
