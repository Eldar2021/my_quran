name: mq_crashlytics
description: MyQuran Crashlytics package
version: 0.1.0+1
resolution: workspace
publish_to: none

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  firebase_crashlytics:

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail:
