import 'package:flutter/material.dart';

class FeedBackSenderView extends StatelessWidget {
  const FeedBackSenderView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('FeedBackSenderView'),
      ),
    );
  }
}
