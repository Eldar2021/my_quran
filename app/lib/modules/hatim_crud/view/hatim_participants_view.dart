import 'package:flutter/material.dart';

class HatimParticipantsView extends StatelessWidget {
  const HatimParticipantsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Hatim Participants'),
      ),
    );
  }
}
