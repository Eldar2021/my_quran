import 'package:flutter/material.dart';

class HatimCrudSuccesView extends StatelessWidget {
  const HatimCrudSuccesView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Hatim Crud Succes'),
      ),
    );
  }
}
