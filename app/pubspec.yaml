name: my_quran
description: A new Flutter project.

publish_to: "none"

version: 1.3.15+25

environment:
  sdk: ">=3.8.0-0 <4.0.0"
  flutter: 3.35.6

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # Firebase (Analytics & Auth )
  firebase_core: ^4.2.0

  # Business Logic & State Management
  flutter_bloc: ^9.1.1
  equatable: ^2.0.7

  # UI & User Interaction
  cupertino_icons: ^1.0.8

  # Networking & Internet
  dio: ^5.9.0

  # Localization
  intl: ^0.20.2

  # Utilities & Helpers
  url_launcher: ^6.3.2
  share_plus: ^12.0.1
  infinite_scroll_pagination: 4.1.0 #5.1.1
  just_audio: ^0.10.5
  just_audio_background: ^0.0.1-beta.17
  loader_overlay: ^5.0.0
  meta: ^1.16.0 #1.17.0

  # snackbar
  toastification: ^3.0.3

  # Navigation
  go_router: ^16.3.0

  # Packages
  mq_ci_keys:
    path: ../packages/mq_ci_keys
  mq_remote_client:
    path: ../packages/mq_remote_client
  mq_storage:
    path: ../packages/mq_storage
  mq_crashlytics:
    path: ../packages/mq_crashlytics
  mq_analytics:
    path: ../packages/mq_analytics
  mq_remote_config:
    path: ../packages/mq_remote_config
  mq_auth_repository:
    path: ../packages/mq_auth_repository
  mq_home_repository:
    path: ../packages/mq_home_repository
  mq_quran_repository:
    path: ../packages/mq_quran_repository
  mq_hatim_repository:
    path: ../packages/mq_hatim_repository
  mq_app_ui:
    path: ../packages/mq_app_ui
  mq_prayer_time:
    path: ../packages/mq_prayer_time

  # flutter_native_splash: ^2.4.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_driver:
    sdk: flutter
  very_good_analysis: ^10.0.0
  test: ^1.25.15 #1.26.3
  mocktail: ^1.0.4
  # flutter_launcher_icons: ^0.14.4

flutter:
  uses-material-design: true
  generate: true

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/app_icon.png"
  # flutter pub run flutter_launcher_icons
